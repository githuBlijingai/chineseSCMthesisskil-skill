# 单片机论文专用配置使用说明

## 概述

针对单片机类论文内容分散、段落均匀、结构松散的问题，skill 新增了专用配置 `chapter-profile-mcu.yaml`。

## 核心改进

| 问题 | 解决方案 |
|------|----------|
| 段落字数几乎一样 | 强制段落长度分布：短(30%):中(50%):长(20%) |
| 内容分散空泛 | 每段强制 ≥2个技术关键词 + ≥1个具体参数 |
| 结构松散 | 专用章节结构 + 逻辑衔接模板 |
| 机械表达 | AIGC风格治理规则 |

## 配置结构

```yaml
chapter-profile-mcu.yaml
├── paragraph_structure      # 段落长度控制
├── content_density_requirements  # 内容密度要求
├── chapters                 # 各章详细配置
│   ├── chapter_1 (绪论)
│   ├── chapter_2 (系统方案)
│   ├── chapter_3 (硬件设计)     # 最详细
│   ├── chapter_4 (软件设计)     # 最详细
│   └── chapter_5 (系统测试)
├── logical_transitions      # 逻辑衔接模板
└── aigc_style_rules         # 风格治理规则
```

## 使用方式

### 方式 1: 自动检测（推荐）

系统自动检测单片机论文特征：

```bash
# 检测是否为单片机论文
python scripts/select_chapter_profile.py --detect-only

# 查看配置详情
python scripts/select_chapter_profile.py
```

检测依据：
- `paper.type_profile` 包含 "mcu" 或 "单片机"
- `paper.type` 包含 "单片机"/"嵌入式"/"STM32"
- `technology_or_method_stack.hardware` 包含单片机相关关键词

### 方式 2: 手动指定

在 `thesis-ai-spec.yaml` 中明确指定：

```yaml
paper:
  type_profile: "mcu_system_design"  # 或包含 "单片机"
  type: "基于STM32的XX系统设计"

chapter_profiles:
  mcu_system_design:
    config_ref: "chapter-profile-mcu.yaml"
```

### 方式 3: AI提示词调用

在生成章节时，使用专用提示词：

```text
Use $chinese-thesis-workbench, 请使用 MCU-3 提示词撰写硬件章节。
```

或完整流程：

```text
Use $chinese-thesis-workbench:
1. 运行 MCU-1 检查当前结构
2. 运行 MCU-2 检查内容密度
3. 运行 MCU-3 生成硬件章节
4. 运行 MCU-4 生成软件章节
5. 运行 MCU-6 进行风格治理
```

## 章节结构对比

### 通用 system_design 结构
```
第1章 绪论
第2章 相关技术与开发环境
第3章 系统需求分析
第4章 系统总体设计
第5章 系统详细实现
第6章 系统测试
第7章 结论与展望
```

### MCU专用结构
```
第1章 绪论
第2章 系统方案与关键技术        ← 强调方案对比和选型
第3章 硬件电路设计              ← 详细展开：主控、电源、传感器、通信、PCB
第4章 软件程序设计              ← 详细展开：环境、架构、模块、中断、协议
第5章 系统测试与验证
第6章 结论与展望
```

## 段落长度控制示例

以 **3.1 主控单元电路** 为例：

```yaml
section_id: "3.1"
title: "主控单元电路"
paragraph_count: 6
paragraph_lengths: [100, 250, 450, 250, 250, 100]
```

对应写作：

| 段落 | 长度 | 内容 | 开头示例 |
|------|------|------|----------|
| P1 | 100字 | 引入：主控芯片作用 | "主控单元是整个系统的核心..." |
| P2 | 250字 | 芯片选型：候选对比 | "在芯片选型时，考虑了XX、YY、ZZ三款..." |
| P3 | 450字 | 详细分析：最终选择理由 | "最终选择STM32F103C8T6，基于以下考虑..." |
| P4 | 250字 | 最小系统电路 | "最小系统电路包括..." |
| P5 | 250字 | 时钟与复位 | "时钟电路采用8MHz外部晶振..." |
| P6 | 100字 | 小结 | "综上所述，主控单元设计..." |

## 技术关键词池

### 硬件类（每段至少2个）
- STM32F103、STM32F407、51单片机
- GPIO、USART、I2C、SPI、ADC、DMA
- 时钟树、复位电路、晶振、滤波电容

### 软件类（每段至少2个）
- 中断服务程序、定时器中断、状态机
- 寄存器配置、HAL库、标准库
- 初始化函数、主循环、看门狗

### 具体参数（每段至少1个）
- 72MHz主频、3.3V供电、5V兼容
- 12位ADC、1ms定时、9600波特率
- 8MHz晶振、10K上拉、100nF退耦

## AIGC风格治理规则

### 禁止表达
- ❌ "首先...其次...再次...最后"
- ❌ "具有重要意义"
- ❌ "具有重要价值"
- ❌ "本文设计的系统"（冗余）

### 推荐开头多样化
- ✅ "为实现XX功能..."
- ✅ "针对XX问题..."
- ✅ "考虑到XX约束..."
- ✅ "测试结果表明..."
- ✅ "与ZZ方案相比..."

## 逻辑衔接模板

### 硬件到软件过渡
```
"硬件电路设计完成后，软件部分需要针对上述硬件平台进行程序开发..."
"基于第3章设计的硬件架构，本章进行相应的软件程序设计..."
```

### 模块间衔接
```
"在上述XX模块的基础上，为实现YY功能，需要..."
"完成XX配置后，下一步是YY的初始化..."
```

### 理论到实践
```
"基于上述原理分析，具体实现方案如下..."
"为验证上述方案的可行性，设计了如下实验..."
```

## 证据绑定要求

每段必须绑定以下证据之一：
- 原理图截图
- 芯片手册引用
- 实物照片
- 代码片段
- 测试数据

无证据的描述必须标记：`[needs_evidence]`

## 快速检查清单

写作完成后，运行以下检查：

```bash
# 检查段落长度分布
python scripts/select_chapter_profile.py

# 手动检查要点
□ 每章是否出现芯片具体型号（至少1次）
□ 每节是否出现具体参数值（至少1次）
□ 相邻段落字数差异是否>30%
□ 是否有"首先...其次...再次...最后"
□ 段落开头是否多样化
□ 是否每段都有技术关键词
```

## 故障排除

### 配置未生效
1. 检查 `thesis-ai-spec.yaml` 是否包含单片机关键词
2. 运行 `python scripts/select_chapter_profile.py` 确认检测正确
3. 检查 `chapter-profile-mcu.yaml` 是否存在

### 内容仍显空泛
1. 确认使用了 MCU-1 ~ MCU-6 专用提示词
2. 检查证据材料是否充足
3. 手动补充具体芯片型号和参数

### 段落仍过于均匀
1. 参考 `paragraph_lengths` 配置手动调整
2. 使用短段落专门用于过渡
3. 长段落用于详细分析和对比

### 硬件证据解析问题
如果你的项目包含PCB、原理图或BOM文件，但未正确解析：

1. 确认硬件文件位于正确目录
   ```
   项目目录/
   ├── hardware/          # 硬件设计文件
   │   ├── *.pcbdoc       # PCB文件
   │   ├── *.schdoc       # 原理图文件
   │   └── BOM.csv        # 物料清单
   └── ...
   ```

2. 运行硬件证据解析
   ```bash
   python scripts/evidence/build_hardware_evidence.py --project-path .
   ```

3. 检查输出
   ```
   paper-context/evidence/
   ├── hardware-evidence.json
   └── hardware-evidence.md
   ```

4. 在论文中引用硬件证据
   - BOM元器件型号 → 章节3.2电源电路、3.3传感器接口
   - PCB尺寸/层数 → 章节3.5 PCB设计
   - 原理图网络 → 章节3.1-3.4 电路描述

## 更新记录

- 2026-05-27: v1.1 新增硬件设计文件解析支持（PCB/原理图/BOM）
- 2026-05-23: 初始版本，解决单片机论文内容分散问题
